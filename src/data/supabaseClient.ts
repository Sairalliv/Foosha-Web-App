// ── Supabase client (placeholder) ─────────────────────────────────
//
// This file is ready for you to activate when you connect Supabase.
//
// Steps:
//   1. npm install @supabase/supabase-js
//   2. Copy .env.example → .env.local and fill in your project URL + anon key
//   3. Uncomment the lines below
//   4. Swap the mock internals in AuthContext.tsx and api.ts to use `supabase`
//
// That's it — the rest of the app talks to api.ts and useAuth(), which
// already have the right shapes for a Supabase-backed implementation.

// import { createClient } from "@supabase/supabase-js";
//
// const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
// const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;
//
// if (!supabaseUrl || !supabaseAnonKey) {
//   throw new Error(
//     "Missing VITE_SUPABASE_URL or VITE_SUPABASE_ANON_KEY in .env.local — " +
//     "see .env.example for the template."
//   );
// }
//
// export const supabase = createClient(supabaseUrl, supabaseAnonKey);
